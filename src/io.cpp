#include "io.h"

vector<string> grep_dir(string path, string &name) {
    vector<string> results;

    /*
    for (const auto & entry : std::filesystem::directory_iterator(path)) {
        results.push_back(entry.path());
    }
    */

    return results;
}
